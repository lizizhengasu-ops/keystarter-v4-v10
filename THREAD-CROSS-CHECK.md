# 线程交叉检查：web-main(11PM-2AM) vs 本线程

## 检查结论：没有冲突。互补关系。

## web-main 线程产出的决策（V2 架构）
| 决策 | 本线程是否一致 | 说明 |
|------|--------------|------|
| 三门户架构（Admin/Tenant/Widget） | ✅ V3-V8 沿用 | 一致 |
| 续费横幅在 Tenant Portal 不在 widget | ✅ V5-V8 沿用 | 一致 |
| 租户选择器仅管理员可见 | ✅ 未变更 | 一致 |
| 物理隔离（独立进程+独立KB目录） | ✅ V4-V8 沿用 | 一致 |
| HMAC token + 域名白名单 | ✅ V4-V8 安全章节 | 一致 |
| SQLite 数据库 | ✅ V4-V8 沿用 | 一致 |
| Stripe 订阅收款 | 🔄 V4 **改为**微信/支付宝 | 这是用户反馈后做的修改，算更新不是冲突 |

## 本线程新增的决策（V3-V8）
| 决策 | 来源 | 说明 |
|------|------|------|
| 两条交付路径（插件+预埋） | V4 | web-main 没讨论过，新增 |
| 7天免费试用 | V5 | web-main 没讨论过，新增 |
| 销售助理提示词框架（SPIN/AIDA/LAARC） | V6-V7 | web-main 没讨论过，新增 |
| 意图识别分类（8类） | V8 | web-main 没讨论过，新增 |
| KB 边界约束（OOS 拒答） | V8 | web-main 没讨论过，新增 |
| 客户 Dashboard 6 个 KB 管理栏目 | V6 | web-main 没讨论过，新增 |

## 唯一需要注意的点
web-main 线程里设计收款用的是 Stripe。本线程 V4 根据你的反馈改成了微信/支付宝。这是**你要求改的**，不是冲突。

## 总体
web-main（11PM-2AM）= 搭好了地基（架构、安全、隔离）
本线程（今天）= 在这地基上建起了房子（销售行为、知识库管理、意图理解、付款方式）

建议：保持现状，两个线程的工作不用合并。
