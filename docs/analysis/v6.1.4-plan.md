# v6.1.4 — 安全加固 + 用户反馈修正

## 修正: 邮件发送量
错误假设: 312 产品 = 312 封邮件 ❌
正确: 100 订单 = 100 封邮件 (每封含该订单所有 Key) ✅

→ PHP 超时问题不存在, 撤回 ❌
→ 测试 2.1 无效, 移除

## v6.1.4 真正需要修复的 4 项

### 修复 1: CSRF 保护 → 导入表单加 nonce

`
表单:
  <form method="post" enctype="multipart/form-data">
    <?php wp_nonce_field('ks_import_csv', 'ks_csv_nonce'); ?>
    <input type="file" name="csv_file" accept=".csv">
    <input type="submit" value="Import CSV">
  </form>

服务端:
  if (!isset(['ks_csv_nonce']) || 
      !wp_verify_nonce(['ks_csv_nonce'], 'ks_import_csv')) {
    wp_die('Security check failed.');
  }
`

### 修复 2: XSS 防护 → 输出转义

`
// Key 显示在管理页面:
  echo esc_html();  // ✅ 转义 HTML

// Key 显示在 input 输入框:
  <input value="<?php echo esc_attr(); ?>">  // ✅ 转义属性

// Key 写入邮件:
   = "<div>" . esc_html() . "</div>";  // ✅ 纯文本
`

### 修复 3: Key 唯一性检查 → 防止同一 Key 发两个客户

`
// 保存 Key 前检查:
 = ->get_var(->prepare(
  "SELECT COUNT(*) FROM {->postmeta} 
   WHERE meta_value = %s 
   AND meta_key LIKE '_license_key_%'",
  
));
if ( > 0) {
  // Key 已分配给其他订单 → 报警
  [] = "Key '' already assigned to another order, skipped";
  continue;
}
`

### 修复 4: 队列模式 ↔ CSV 模式互斥 → 订单级锁

`
// 处理订单前加锁:
 = "ks_order_lock_{}";
if (wp_cache_add(, true, '', 30)) {
  // 获取锁成功 → 处理
  process_order();
  // 处理完成后释放
  wp_cache_delete();
} else {
  // 订单正在被其他管理员处理 → 跳过
  [] = "Order #{} is being processed by another admin";
}
`

---

## v6.1.4 模拟测试 (纠正后)

| # | 测试 | v6.1.3 | v6.1.4 | 修复 |
|:-:|------|:------:|:------:|------|
| 1 | BOM 头部 | ✅ | ✅ | 已有 |
| 2 | Key 为空跳过 | ✅ | ✅ | 已有 |
| 3 | 订单不存在 | ✅ | ✅ | 已有 |
| 4 | Slug 不匹配 | ✅ | ✅ | 已有 |
| 5 | 重复行 | ✅ | ✅ | 已有 |
| 6 | 特殊字符 | ✅ | ✅ | 已有 |
| 7 | Brevo API 中断 | ✅ | ✅ | 已有 |
| 8 | 网络断开 | ✅ | ✅ | 已有 |
| 9 | 空 Key 发送 | ✅ | ✅ | 已有 |
| 10 | 订单冲突 | ✅ | ✅ | 已有 |
| 11 | 无效邮箱 | ✅ | ✅ | 已有 |
| 12 | Key 超额 | ✅ | ✅ | 已有 |
| 13 | CSRF 保护 | ❌ | ✅ | 修复 1 |
| 14 | XSS 转义 | ❌ | ✅ | 修复 2 |
| 15 | Key 重复分配 | ❌ | ✅ | 修复 3 |
| 16 | 模式互斥 | ❌ | ✅ | 修复 4 |
| 17 | PHP 超时 | ❌ (误判) | N/A | 已撤回 |

**v6.1.4 通过全部 16 项测试 ✅ (第 17 项已确认为误判)**
