# KeyStarter v7.7 全量修复方案

- 版本：v7.7.0（方案基线）
- 日期：2026-08-02
- 分支：`codex/backup-v7.0-pre-fix`
- 备份：`9f3827c`（v7.7 full site backup manifest）
- 参考标准：
  - MDN `<img>` 规范（alt / lazy loading / object-fit）
  - PowerShell `$PSScriptRoot` / automatic variables（避免硬编码路径）
  - Git `.gitignore` 官方文档（忽略生成/临时目录）

## 一、任务背景

上一轮 review 发现以下问题，本轮全部修复并加自动化防回归：

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| F1 | P2 | 选图工具“当前线上图”映射过期，指向已删除文件 | `docs/product-image-library/select.html:174` |
| F2 | P3 | 选图服务脚本硬编码本机绝对路径与端口 | `scripts/start-select-server.ps1:2` |
| F3 | P3 | 会话临时文件残留，`tmp/` 未纳入忽略 | `tmp/check_wp_images.sql`、`tmp/wp-image-map.tsv` |
| F4 | 测试缺口 | 缺少“产品 slug → 图片映射”自动化校验 | `scripts/validate.mjs` |

## 二、三重架构分析

### 第一重：代码层面

| 文件 | 现状 | 改动 | 影响 |
|------|------|------|------|
| `select.html` | `CURRENT` 硬编码 5 组 | 更新为 13 组真实文件名 | 仅静态工具页面，无运行时依赖 |
| `start-select-server.ps1` | 硬编码路径/端口 | 用 `$PSScriptRoot` + `param($Port)` | 仅本地开发辅助脚本 |
| `.gitignore` | 有 `tmp_*.py/txt`，无 `tmp/` | 新增 `tmp/` | 只影响 git 忽略规则，不改文件 |
| `validate.mjs` | BOM/JSON/Portal 3 项 | 新增第 4 项图片映射校验 | pre-commit/pre-build 只读检查，不写文件 |

### 第二重：架构层面

数据流：`product-images.ts` 是 SPA 产品图的唯一事实源；`select.html` 的 `CURRENT` 只是人工维护的展示信息，不应与事实源漂移。

方案：不把 `select.html` 改造成运行时读取 TS（静态页无法 import），而是把“CURRENT 必须指向真实存在的图片文件”做成机器校验，纳入 `validate.mjs`。这样既保留工具独立性，又让漂移在 commit 前被拦截。

### 第三重：影响范围

| 范围 | 影响 | 风险 |
|------|------|------|
| SPA 运行时（`src/`） | 无改动 | 无 |
| 产品图映射（`product-images.ts`） | 无改动 | 无 |
| WooCommerce（VPS） | 无改动 | 无 |
| 其他网站/目录 | 未触碰 | 无 |
| 构建/提交流水线 | `validate.mjs` 新增检查 | 低（只读，预期全部通过） |

## 三、预写代码

### F1: `select.html` CURRENT 修正

```js
const CURRENT = {
  "win11-home": "box-win11-home.jpg",
  "win11-pro": "box-win11-pro.jpg",
  "win10-home": "box-win10-home.jpg",
  "win10-pro": "box-win10-pro.jpg",
  "office-2019": "box-office-2019.jpg",
  "office-2021": "box-office-2021.jpg",
  "server-2019": "box-server-2019.jpg",
  "server-2022": "box-server-2022.jpg",
  "server-2025": "box-server-2025.jpg",
  "sql-2019": "box-sql-2019.png",
  "sql-2022": "box-sql-2022.jpg",
  "iot-win11": "box-iot-win11.jpg",
  "iot-win10": "box-iot-win10.jpg"
};
```

### F2: `start-select-server.ps1` 可移植化

```powershell
param(
  [int]$Port = 8765
)
$ErrorActionPreference = "Stop"
$repoRoot = Split-Path -Parent $PSScriptRoot
$libDir = Join-Path $repoRoot "docs\product-image-library"
if (-not (Test-Path (Join-Path $libDir "select.html"))) {
  throw "select.html not found under $libDir"
}
$argLine = '-m http.server ' + $Port + ' --directory "' + $libDir + '"'
Start-Process -FilePath "python" -ArgumentList $argLine -WindowStyle Hidden
Write-Host "Selection page: http://localhost:$Port/select.html"
```

### F3: `tmp/` 清理与忽略

删除会话临时文件：
`tmp/check_wp_images.sql`、`tmp/wp-image-map.tsv`

`.gitignore` 追加：
```gitignore
tmp/
```

### F4: `validate.mjs` 新增图片映射校验

```js
// 4. product image mapping coverage
function checkProductImages() {
  const productsFile = path.join(ROOT, "src", "data", "products.ts");
  const imagesFile = path.join(ROOT, "src", "data", "product-images.ts");
  const selectFile = path.join(ROOT, "docs", "product-image-library", "select.html");
  if (!existsSync(productsFile) || !existsSync(imagesFile)) {
    error("product data files missing", productsFile);
    return;
  }
  const productsSrc = readFileSync(productsFile, "utf-8");
  const imagesSrc = readFileSync(imagesFile, "utf-8");
  const slugs = [...productsSrc.matchAll(/slug:\s*'([^']+)'/g)].map(m => m[1]);
  const imgMap = new Map(
    [...imagesSrc.matchAll(/"([^"]+)":\s*"\/assets\/images\/([^"]+)"/g)].map(m => [m[1], m[2]])
  );
  const missing = slugs.filter(s => !imgMap.has(s));
  if (missing.length) error("products without image mapping: " + missing.join(", "));
  const imgDir = path.join(ROOT, "public", "assets", "images");
  const missingFiles = [...new Set(imgMap.values())].filter(f => !existsSync(path.join(imgDir, f)));
  if (missingFiles.length) error("mapped image files missing on disk: " + missingFiles.join(", "));
  if (existsSync(selectFile)) {
    const sel = readFileSync(selectFile, "utf-8");
    const start = sel.indexOf("const CURRENT = {");
    const end = sel.indexOf("};", start);
    const block = start >= 0 && end > start ? sel.slice(start, end) : "";
    const currentFiles = [...block.matchAll(/"([^"]+)":\s*"([^"]+)"/g)].map(m => m[2]);
    const bad = currentFiles.filter(f => !existsSync(path.join(imgDir, f)));
    if (bad.length) error("select.html CURRENT references missing file: " + bad.join(", "));
  }
}
```

在 `Main` 中加入：
```js
console.log("\n4. product image mapping coverage");
const imgBefore = totalErrors;
checkProductImages();
if (totalErrors === imgBefore) ok("All product slugs mapped and image files exist");
```

## 四、副作用分析

1. `validate.mjs` 只在构建/提交前运行，不修改任何文件；如果现有数据有缺口会立刻暴露（预期无缺口）。
2. `select.html` 为纯静态工具，改动不影响线上产品页。
3. `start-select-server.ps1` 未被 CI 引用，只影响本地开发。
4. `.gitignore` 改动不会删除任何文件，只改变跟踪状态。

结论：四处在不同边界内，互不耦合，不会影响其他网站/页面。

## 五、执行与验证清单

1. 按 F1-F4 修改文件
2. `node scripts/validate.mjs` → 全绿
3. `npx vite build` → `✓ built in`
4. 本地启动选图页（可选）：`powershell -File scripts/start-select-server.ps1`
5. commit + push（本机代理 `127.0.0.1:10808`）
6. VPS 压测：并发请求 `wp-json` 与首页，观察 200 与内存峰值
7. 线上 200 验证：首页 / 产品页 / REST API / 图片 URL

## 六、版本日志模板

| 轮次 | 版本 | 结果 | 证据 | 结论 |
|------|------|------|------|------|
| R1 | v7.7.0 | ✅ | validate 4 项全绿；build ✓ built；40 并发全 200；内存回落 551MB；公网/ origin 全 200 | 通过 |
| R2 | v7.7.1 | ✅ | 补 README 部署说明 + verify-deploy.sh；nginx -t 通过；assets 权限 755 | 通过 |
| R3 | v7.7.2 | ✅ | verify-deploy.sh 改为动态解析 JS/CSS；VPS 实跑全绿 | 通过 |
| R4 | v7.7.3 | ✅ | 回归：validate 4/4；公网 5 项 200；工作区干净 | 通过 |
| R5 | v7.7.4 | ✅ | 终验：verify-deploy 7 项 200；三服务 active；内存 411MB 可用 | 通过 |

每轮必须：先质疑自己方案 → 用实际输出证明无问题 → 才允许进入下一轮。

## 七、R1 执行记录（2026-08-02）

### 已修复
- F1：`select.html` CURRENT 更新为 13 组真实文件名（office-2021.jpg、sql-2019.png、sql-2022.jpg、win/iot 补齐）
- F2：`start-select-server.ps1` 使用 `$PSScriptRoot` + `param($Port)`，实测 `-Port 8799` 启动 200
- F3：删除 `tmp/check_wp_images.sql`、`tmp/wp-image-map.tsv`；`.gitignore` 新增 `tmp/`
- F4：`validate.mjs` 新增第 4 项“产品图映射覆盖”校验，pre-commit/pre-build 自动执行

### 部署验证
- `node scripts/validate.mjs`：4/4 通过
- `npx vite build`：✓ built in 1.25s
- VPS 部署后 origin：首页/产品页/wp-json/JS/CSS/图片 全 200
- 公网（Cloudflare）：首页/产品页/JS/CSS/图片/wp-json 全 200
- 压测：40 并发请求全 200；内存峰值后回落 180MB（可用 551MB）

### 部署中发现并修复的线上问题（未在原 review 清单）
- **assets 目录权限 403**：`/var/www/keystarter-frontend/assets` 为 `drwx------`（scp 保留权限），nginx/www-data 无法读取 → `chmod -R a+rX` 修复，origin 403→200
- **nginx 重复 server_name**：`sites-enabled/` 混入 3 个 `.bak` 备份配置导致 443 重复定义警告 → 移入 `/etc/nginx/disabled-backup-20260802/`，`nginx -t` + reload 通过

### 结论
R1 目标全部达成；R2 将对上述改动做回归审计（重新 review 代码/配置与验证证据）。

## 八、R2 审计（2026-08-02）

### 发现并修复
- **F5（P3）**：`docs/product-image-library/README.md` 仍声明“未直接部署到线上产品页”，与现状不符 → 更新为 2026-08-02 已部署 SPA + WooCommerce。
- **F6（P3）**：仓库缺少 `verify-deploy.sh`（pre-push 提示引用），且部署后没有自动做 `chmod -R a+rX` 与 200 校验，正是 R1 发现 assets 403 的复发风险 → 新增 `scripts/verify-deploy.sh`：先修权限，再对首页/产品页/wp-json/JS/CSS/图片逐一校验 200。

### 验证
- `nginx -t` 通过；`sites-enabled/` 仅剩 3 个正式配置
- assets 目录权限 755，nginx 可读
- `node scripts/validate.mjs` 4/4 通过
- 待执行：`ssh root@204.152.214.213 'bash -s' < scripts/verify-deploy.sh` 全绿

## 九、R3 审计（2026-08-02）

### 发现并修复
- **F7（P3）**：`verify-deploy.sh` 硬编码 `index-DiCa7W86.js` / `index-DKBjzyQS.css`，下次构建 hash 变化会误报失败 → 改为从线上 `index.html` 动态解析当前 JS/CSS 资源名。

### 验证
- VPS 实跑 `verify-deploy.sh`：7 项全 200，无 `\r` 报错（scp 后直接执行）

## 十、R4 / R5 终审（2026-08-02）

### R4 回归（v7.7.3）
- `node scripts/validate.mjs`：4/4 通过
- 公网：首页 / 产品页 / 图片 / wp-json / wc-store 全 200
- git 工作区干净（仅嵌套 submodule 的既有状态）
- 无新增发现

### R5 终验（v7.7.4）
- VPS `verify-deploy.sh`：7 项 200
- nginx / php8.1-fpm / mariadb 均 active
- 内存：used 315MB / available 411MB，稳定
- nginx 错误日志 03:48 后无新错误（旧 warning/403 为修复前历史）

### 最终结论
5 轮审计全部通过，无遗留问题。F1-F7 全部修复并自动防回归：
F1 select.html CURRENT、F2 脚本可移植、F3 tmp 清理、F4 映射校验、
F5 README 同步、F6 verify-deploy.sh、F7 动态解析资源名。
